import { createNativeStackNavigator } from '@react-navigation/native-stack';

import { DefaultParamList } from 'main/router/DefaultParamList';

export const Stack = createNativeStackNavigator<DefaultParamList>();
